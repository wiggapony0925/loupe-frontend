# Caches & builds
node_modules/
build/
dist/
coverage/
*.log
*.tsbuildinfo
.eslintcache

# Expo / Metro
.expo/
.expo-shared/
web-build/
.metro-cache/

# Native build outputs — anchored to repo root so modules/*/ios + android stay tracked
/ios/
/android/
*.xcworkspace
*.xcuserdata/
DerivedData/

# Lockfiles ignored from formatting only (keep tracked in VCS)

# Env / secrets
.env
.env.*
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json
.idea/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
expo-env.d.ts
# @end expo-cli

# Fetched TCG set artwork (Pokémon TCG / Scryfall). ~21MB across ~350 PNGs.
# Tracked in git so EAS Build / CI bundling can resolve the require() paths in
# src/shared/setLogos.generated.ts without hitting external APIs server-side.
# To refresh: `node scripts/fetch-set-logos.mjs && node scripts/generate-set-logo-registry.mjs`
