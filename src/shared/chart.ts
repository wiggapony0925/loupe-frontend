/**
 * @deprecated Import from `@/domain/charts` instead.
 *
 * This file is a back-compat re-export shim. New code should import
 * `monotoneCubic`, `nearestIndex`, `clampLabelX` (and friends) from
 * `@/domain/charts` directly.
 */
export {
  clampLabelX,
  monotoneCubic,
  nearestIndex,
} from "@/domain/charts";
